"""
Django AppConfig for the 'recipies' app.

This module defines the configuration for the 'recipies' app, including its name.
"""

from django.apps import AppConfig


class RecipiesConfig(AppConfig):
    """
    Django AppConfig for the 'recipies' app.

    This class defines the configuration for the 'recipies' app, including its name.
    """

    name = 'recipies'
